model Category {
  id        String   @id @default(cuid())
  name      String   @unique
  image     String
  slug      String   @unique
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())

  meals     Meal[]
}